CREATE TABLE clams{
    PRIMARY KEY(clamId),
    rakerId INTEGER,
    lat DECIMAL,
    long DECIMAL,
    length INTEGER,
    width INTEGER,
    weight INTEGER,
    comments VARCHAR(100),
    image VARCHAR(100),
};
