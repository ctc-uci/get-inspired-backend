CREATE TABLE clams{
    clamId INTEGER,
    rakerId INTEGER,
    lat DECIMAL,
    long DECIMAL,
    length INTEGER,
    width INTEGER,
    weight INTEGER,
    comments VARCHAR(1000),
    image VARCHAR(1000),
    PRIMARY KEY(clamId),
};
