CREATE TABLE survey(
  id INTEGER PRIMARY KEY AUTO_INCREMENT,
  `Date` DATE,
  `Beach` TEXT,
  `Location` TEXT,
  `Method` TEXT,
  `Start Time` TIME,
  `Duration` INTEGER,
  `Water Depth` DOUBLE,
  `Distance` DOUBLE,
  `Slope` DOUBLE,
  `Tide` TEXT
);
